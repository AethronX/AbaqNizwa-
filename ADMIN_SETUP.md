# تفعيل لوحة تحكم عبق نزوى المنفصلة

هذا الدليل يشرح خطوات التفعيل التي تنفذها أنت يدوياً في حساب Vercel (لا يمكن تنفيذها من الكود وحده).

## الفكرة العامة

- المتجر الرئيسي (`abaq-nizwa.vercel.app`) أصبح يحتوي على خادم API حقيقي (مجلد `/api`) متصل بقاعدة بيانات Postgres حقيقية بدل التخزين المحلي في متصفح الزائر.
- لوحة التحكم أصبحت تطبيقاً منفصلاً بالكامل (مجلد `/admin` في نفس المستودع) يُنشر كمشروع Vercel ثانٍ له رابطه الخاص، ويتواصل مع نفس الـ API لعرض والتحكم بنفس البيانات الحقيقية.
- عند شراء نطاق مخصص لاحقاً (مثل `abaqnizwa.com`) يمكن ربط `admin.abaqnizwa.com` بمشروع لوحة التحكم مباشرة.

## الخطوة 1: توليد كلمة مرور الأدمن

على جهازك (أو في أي بيئة فيها Node.js)، من داخل المستودع:

```bash
npm install
node scripts/generate-admin-hash.mjs "كلمة-مرور-قوية-من-اختيارك"
```

سيطبع لك نصاً طويلاً يبدأ بـ `$2b$...` — هذا هو `ADMIN_PASSWORD_HASH` الذي ستحتاجه في الخطوة التالية. احتفظ بكلمة المرور الأصلية جانباً — هي التي ستستخدمها لتسجيل الدخول لاحقاً، وليس الهاش.

## الخطوة 2: ربط قاعدة بيانات حقيقية بالمشروع الرئيسي

1. افتح مشروع `abaq-nizwa` في لوحة Vercel.
2. اذهب إلى تبويب **Storage**.
3. اضغط **Connect Database** واختر أي مزوّد Postgres متاح (Neon أو Supabase أو غيرهما — الخادم يعمل مع أي منهم).
4. أنشئ قاعدة بيانات جديدة واربطها بالمشروع — سيضيف Vercel تلقائياً متغيرات بيئة مثل `DATABASE_URL` بدون أي إعداد إضافي منك.

لا تحتاج لتشغيل أي SQL يدوياً — الخادم ينشئ الجداول تلقائياً عند أول طلب (الملف `api/schema.sql` موجود فقط كمرجع للاطلاع).

## الخطوة 3: متغيرات البيئة على المشروع الرئيسي

في **Settings → Environment Variables** لمشروع `abaq-nizwa`، أضف:

| المتغير | القيمة |
|---|---|
| `JWT_SECRET` | نص عشوائي طويل وسري (مثلاً ناتج `openssl rand -hex 32`) |
| `ADMIN_USERNAME` | اسم مستخدم الأدمن الذي تريده |
| `ADMIN_PASSWORD_HASH` | الناتج من الخطوة 1 |
| `SITE_ORIGIN` | `https://abaq-nizwa.vercel.app` (رابط المتجر نفسه) |
| `ADMIN_ORIGIN` | رابط مشروع لوحة التحكم — أضفه بعد الخطوة 4 |

بعد الحفظ، شغّل **Redeploy** لهذا المشروع حتى تُطبَّق المتغيرات.

## الخطوة 4: نشر لوحة التحكم كمشروع منفصل

1. من لوحة Vercel اضغط **Add New → Project**.
2. اختر نفس مستودع GitHub (`AethronX/AbaqNizwa-`).
3. في إعدادات الاستيراد، غيّر **Root Directory** إلى `admin`.
4. أضف متغير البيئة `VITE_API_BASE_URL` = `https://abaq-nizwa.vercel.app` (رابط المتجر الرئيسي من الخطوة السابقة).
5. اضغط **Deploy**. سيعطيك Vercel رابطاً جديداً مثل `abaqnizwa-admin.vercel.app` — هذا هو رابط لوحة التحكم.

## الخطوة 5: إكمال الربط

ارجع لمشروع `abaq-nizwa` الرئيسي وأضف/حدّث متغير `ADMIN_ORIGIN` بالرابط الذي حصلت عليه في الخطوة 4 (مثل `https://abaqnizwa-admin.vercel.app`)، ثم أعد نشر المشروع الرئيسي مرة أخرى (Redeploy) حتى يسمح الخادم لطلبات لوحة التحكم.

## الخطوة 6: أول دخول وتحميل البيانات

1. افتح رابط لوحة التحكم وسجّل الدخول باسم المستخدم وكلمة المرور من الخطوة 1.
2. ستظهر رسالة أن قاعدة البيانات فارغة مع زر **"تحميل البيانات الأولية"** — اضغطه لنقل كتالوج المنتجات وأكواد الخصم الحالية إلى قاعدة البيانات الجديدة.
3. من الآن فصاعداً، أي طلب حقيقي من المتجر (عبر واتساب) وأي تعديل من لوحة التحكم يُسجَّل في نفس قاعدة البيانات، وتظهر التحليلات (الزيارات، المبيعات، أكثر المنتجات طلباً، المدن، الأجهزة...) تلقائياً في تبويب "التحليلات والمؤشرات".

## لاحقاً: عند شراء نطاق مخصص

عند شراء نطاق مثل `abaqnizwa.com`:

1. أضِفه لمشروع المتجر الرئيسي (Settings → Domains) — مثلاً كنطاق رئيسي `abaqnizwa.com` أو `www.abaqnizwa.com`.
2. أضِف `admin.abaqnizwa.com` كنطاق لمشروع لوحة التحكم بنفس الطريقة.
3. حدّث `SITE_ORIGIN` في المشروع الرئيسي و`VITE_API_BASE_URL` في مشروع لوحة التحكم بالروابط الجديدة، وحدّث `ADMIN_ORIGIN` في المشروع الرئيسي لرابط `admin.abaqnizwa.com`.
4. أعد نشر المشروعين.

## ملاحظة أمنية

- رابط لوحة التحكم لن يظهر في أي مكان داخل المتجر العام — لا رابط ولا زر يشير إليه، فقط من يعرف الرابط مباشرة يصل لصفحة الدخول، وهي محمية بكلمة مرور فوق ذلك.
- اختر كلمة مرور قوية وفريدة، ولا تشارك رابط لوحة التحكم إلا مع من تثق بهم لإدارة المتجر.
